let initialState = {
    sitebar: [
        { id: 1, name: "Frend 01" },
        { id: 2, name: "Frend 02" },
        { id: 3, name: "Frend 03" }
    ]
}

const sitebarReducer = (state = initialState) =>{
    return state;
}

export default sitebarReducer